<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/styles/styles.css" />
  </head>
  <body>
    <%- include('partials/header.ejs') %>
    <div class="container">
        <div class="px-4 pt-5 my-5 text-center ">
            <h1 class="display-4 fw-bold text-body-emphasis">Welcome to your own  Blog</h1>
            <div class="col-lg-6 mx-auto">
              <p class="lead mb-4">Feel free to use this page to write down your thoughts and save them for the future!</p>
              <% if (titleArr.length === 0 && !isOpen) { %>
                <h2>Seems like there are no entries for your Blog yet</h2>
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mb-5">
                    <form action="/submit" method="POST">
                        <input type="submit" class="btn btn-primary btn-lg px-4 me-sm-3" value="Create new Post" />
                      </form>
                    <a href="/previous" class="btn btn-outline-secondary btn-lg px-4">Previous Posts</a>
                  </div>
                
            <% } else if (!isOpen) { %> 
                <h2> You have <%= titleArr.length %> entries</h2>
                <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mb-5">
                    <form action="/submit" method="POST">
                        <input type="submit" class="btn btn-primary btn-lg px-4 me-sm-3" value="Create new Post" />
                      </form>
                    <a href="/previous" class="btn btn-outline-secondary btn-lg px-4">Previous Posts</a>
                  </div>
                <% } %>
        </div>
            <% if (isOpen) { %>
                <form action="/submit" method="POST">
                  <div class="input-group mb-3 half">
                    <span class="input-group-text" id="basic-addon1">Title</span>
                    <input
                      type="text"
                      class="form-control"
                      name="contentTitle"
                      aria-label="title"
                      aria-describedby="basic-addon1"
                      value="<%= titleContent %>"
                    />
                  </div>
                  <div class="input-group mb-5">
                    <textarea
                      class="form-control"
                      name="content"
                      aria-label="With textarea"
                    >
            <%= content %></textarea
                    >
                  </div>
                  <div class="d-grid gap-2 d-sm-flex justify-content-sm-center mb-5">
                  <input type="submit" class="btn btn-success px-4" value="Save Post" />
                </form>
                <a href="/previous" class="btn btn-info px-4">Previous Posts</a>
            </div>
            
                <% } %>
          </div>
</div>
</body>
<%- include('partials/footer.ejs') %>

</html>
